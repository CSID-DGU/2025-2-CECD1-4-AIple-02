## Dataset 설명

- `main_pipeline.py` 실행 전에 경로와 API Key를 먼저 설정해야 합니다.
- 설정이 완료된 후 `main_pipeline.py`를 실행하면 데이터셋이 자동으로 구축됩니다.

폴더 구성은 아래와 같습니다:

- `dataset_backup/`  
   프로젝트 진행 과정에서 생성된 실제 데이터셋이 저장된 폴더입니다.

- `dataset_test/`  
   테스트용으로 별도로 분리된 MBTIBench 샘플 데이터셋(1개)입니다.